# 改动日志

## 2026-05-15 - 修复命名空间成员批量添加异常

- 问题：批量添加命名空间成员时，如果请求中的 namespace slug 不存在，服务层可能继续使用空命名空间对象，导致接口返回不明确的服务端错误。
- 修复：在 `NamespacePortalCommandAppService` 中统一使用 `requireNamespace(slug)` 查询命名空间；查不到时抛出 `DomainBadRequestException("error.namespace.slug.notFound", slug)`，让调用方得到明确的 400 业务错误。
- 测试：新增缺失 namespace 的批量添加成员用例，并将空成员数组请求断言为 400。
- 配置：本地后端端口统一调整为 `8082`，同步更新 `application-local.yml`、`.env.release.example` 和前端 Vite 代理配置。
